import 'package:flutter/cupertino.dart';

class UserVideosScreen extends StatelessWidget {
  const UserVideosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('User Videos Screen'));
  }
}
